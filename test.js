import mongoose from 'mongoose'; mongoose.connect('mongodb+srv://sandalanithinkumar123_db_user:2AZRYrMEtWCBiZKL@sugartimes.kdostow.mongodb.net/'); const Payment = mongoose.model('Payment', new mongoose.Schema({ amount: Number, status: String }, {strict:false}), 'payments'); const r1 = await Payment.aggregate([{ \$group\: { _id: null, total: { \$sum\: '\$amount\' } } }]); const r2 = await Payment.aggregate([{ \$match\: {status: 'success'} }, { \$group\: { _id: null, total: { \$sum\: '\$amount\' } } }]); console.log('All:', r1, 'Success:', r2); process.exit(0);
